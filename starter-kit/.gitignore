# ★★ 這個資料夾【還不是】git repo。強烈建議接上 —— 你會需要「回到昨天那個能跑的版本」。
#
#    ⚠️ git 不預裝在 Windows 上，而且全新的機器要先設身分，否則 commit 會失敗。
#       完整步驟（含裝 git、設 user.name/email）在 START-HERE.md 的環境檢查表第 5 項。
#
#    ⚠️ PowerShell 5.1 【不支援 &&】—— 一行一行跑，不要串起來：
#
#        git init
#        git add -A
#        git commit -m "起手式"
#
#    這份 .gitignore 已經替你設定好該忽略什麼了。

# ---------------------------------------------------------------------------
# ★ place/*.rbxl 【要】進版控 —— 沒有寫在這裡忽略它，是故意的
#
#   它是二進位檔（git 看不懂內容、diff 不出東西、合併不了），
#   但它是你的 MeshPart 模型與地形的【唯一】家 —— Rojo 同步不了這些東西。
#   不進版控 = 一次誤刪就全沒了。進版控雖然醜，但至少救得回來。
#
#   ⇒ 一個人做就進版控。多人同時改同一個 .rbxl 會衝突且無法合併 —— 別這樣做。
# ---------------------------------------------------------------------------

# Roblox 場景檔的自動備份與鎖檔（這些才要忽略）
place/*.rbxl.lock
*.rbxl.bak

# Rojo 的建置產物（★ 只忽略 check* 開頭的，不要整片忽略 *.rbxlx ——
#   萬一使用者在 Save As 時選成 XML 格式存成 .rbxlx，他的模型和地形會【安靜地】被排除在 git 之外，
#   而 git 在這裡存在的唯一理由就是「救回昨天那個版本」）
check*.rbxl*

# 個人筆記（永不入版控）
project-note.html
.env
